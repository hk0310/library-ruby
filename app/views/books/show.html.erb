<h1><%= @book.title %></h1>

<img src="<%= @book.cover_image_url.nil? ? "/images/no_cover.png": @book.cover_image_url %>" alt="book-cover">

<h2>By <i><%= link_to @book.authors[0].name, @book.authors[0] %></i></h2>
<p>ISBN: <%= @book.isbn %></p>
<p>Published on <%= @book.published_date %>, <%= @book.page_count %> pages</p>
<p>Genres: 
<% @book.genres.each do |genre| %>
    <%= link_to genre.name, genre %>,
<% end %>
</p>
<p><%= @book.synopsis %></p>
