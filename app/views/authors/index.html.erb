<h1>Authors</h1>
<ul>
    <% @authors.each do |author| %>
        <li>
            <img src="<%= author.photo_url %>" width="65">
            <h3><%= link_to author.name, author %></h3>
        </li>
    <%end%>
</ul>
